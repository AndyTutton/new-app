<div class="p-3 ">
  <div class="grid gap-8 sm:grid-cols-2">

    <% @experiences.each do |experience| %>
      <div>
        <%= link_to experience_path(experience) do %>
          <% experience.images.each do |image| %>
            <%= image_tag url_for(image), class: " rounded-lg" %>
          <% end %>
          <div class="row"></div>
            <h2 class="pt-3 font-bold"><%= experience.title %></h2>
            <div class="flex justify-between pb-4">
              <p class="text-left"><%= experience.country %>, <%= experience.region %></p>
              <p class="text-right">€<%= experience.nightly_price %>/night</p>
            </div>

        <% end %>
      </div>
    <% end %>
  </div>

  </div>
